# Loss Registry

`mlxsmith losses` prints the registered loss functions available for preference and RL training.

## Usage

```bash
mlxsmith losses
```

Example output:

```text
Registered Loss Functions
Name    Function
----    --------
...     ...
```

The list is populated from `mlxsmith.sdk.losses` and includes both preference and RL losses.
